/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BooleanResponse } from '../../models/boolean-response';
import { StoryUpdateResource } from '../../models/story-update-resource';

export interface UpdateStory$Params {

/**
 * The user story id
 */
  'user-story-id': number;
      body: StoryUpdateResource
}

export function updateStory(http: HttpClient, rootUrl: string, params: UpdateStory$Params, context?: HttpContext): Observable<StrictHttpResponse<BooleanResponse>> {
  const rb = new RequestBuilder(rootUrl, updateStory.PATH, 'patch');
  if (params) {
    rb.path('user-story-id', params['user-story-id'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BooleanResponse>;
    })
  );
}

updateStory.PATH = '/api/v1/user-stories/{user-story-id}';
